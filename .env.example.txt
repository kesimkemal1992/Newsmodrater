# ─── Telegram User Account ─────────────────────────────────────────────────────
# Get API_ID and API_HASH from https://my.telegram.org/apps
TELEGRAM_API_ID=12345678
TELEGRAM_API_HASH=your_api_hash_here
TELEGRAM_PHONE=+251911234567          # Your phone number (international format)
SESSION_NAME=manager_session          # Session file name (keep consistent)

# ─── Channels ──────────────────────────────────────────────────────────────────
# Comma-separated list of source channel usernames or IDs to monitor
SOURCE_CHANNELS=@channel1,@channel2,@channel3

# Your destination channel (must be admin with post permission)
DEST_CHANNEL=@your_channel

# ─── AI Keys ───────────────────────────────────────────────────────────────────
GEMINI_API_KEY=your_gemini_api_key_here    # https://aistudio.google.com/
GROQ_API_KEY=your_groq_api_key_here        # https://console.groq.com/

# ─── Channel Category (AI filter context) ──────────────────────────────────────
CHANNEL_CATEGORY=Forex trading, gold (XAU/USD), silver, WTI oil, US indices (US30 NAS100), crypto BTC ETH, macroeconomic news

# ─── Timing ────────────────────────────────────────────────────────────────────
POLL_INTERVAL=60          # How often to check for new messages (seconds)
MIN_DELAY=8               # Min delay before posting (human behaviour)
MAX_DELAY=30              # Max delay before posting (human behaviour)
LOOKBACK_HOURS=2          # How far back to look on first run

# ─── Memory ────────────────────────────────────────────────────────────────────
DB_PATH=memory.db
HASH_TTL_DAYS=30          # How long to remember seen content
